machine:
  java:
    version: oraclejdk8

  services:
    - docker

dependencies:
  override:
    - ./gradlew dependencies

test:
  override:
    - ./gradlew test

deployment:
  master:
    branch: master
    owner: stormcat24
    commands:
      - ./gradlew build
      - docker build -t stormcat24/elastiquartz:latest .

